import React from 'react';

const Connections = () => (
  <div>
    <h1>Connections</h1>
    <p>Select what you want to connect to.</p>
    {/* Add connection form or selection options here */}
  </div>
);

export default Connections;